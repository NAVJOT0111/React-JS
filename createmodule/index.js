import {sumData as sum, minData, cName} from "./calculator.js";               //use curly braces for named exports

console.log(sum(10,20));                                                 // using aliasing
console.log(minData(40,20));
console.log(cName);